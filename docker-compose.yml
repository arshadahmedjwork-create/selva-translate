version: '3.8'

services:
  web:
    build: .
    container_name: translation_web
    restart: always
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - TELEGRAM_BOT_TOKEN=8717998365:AAH3i9-lR0IXNIQN3owvoQvZ5-kNsHn_lEg
      - HUGGINGFACE_API_TOKEN=${HUGGINGFACE_API_TOKEN}
